# HouseQA 環境變數範例
# 複製成 .env 並填入實際值（.env 已被 .gitignore 排除，不會上傳）。

# 未來「AI 差異分析」功能使用（接 Anthropic Claude）。目前非必要。
ANTHROPIC_API_KEY=

# 可選：覆寫 data.json 來源（github / local / 一個網址 / 本地路徑）。
# 留空則依 config.yaml 設定。
HOUSEQA_DATA_SOURCE=
